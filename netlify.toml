[build]
  base    = "."
  command = "npm run build"
  functions = "functions"
  publish = "dist"
[dev]
  functionsPort = 34567
[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200
